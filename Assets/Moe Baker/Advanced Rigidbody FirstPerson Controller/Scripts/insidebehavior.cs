using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class insidebehavior : MonoBehaviour
{
    public Slider sl;
    float health12=0;
    // Use this for initialization
    void Start()
    {

        //health12 = PlayerPrefs.GetFloat("Health");

    }

    // Update is called once per frame
    void Update()
    {
        /*sl.value = health12;
        health12 -= 0.001f;
        if (Input.GetKey(KeyCode.I))
        {
            PlayerPrefs.SetFloat("Health", health12);
        }*/
    }
}

		
